name: Mirror to HuggingFace

on:
  push:
    branches:
      - main

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Push to HuggingFace
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          mkdir -p /tmp/hf-push
          cp README.md LICENSE /tmp/hf-push/
          cp -r skills/ /tmp/hf-push/
          cp -r skills/ /tmp/hf-push/plugins-skills/
          cp -r data /tmp/hf-push/

          # Add HF dataset card metadata before README
          printf '%s\n' '---' 'license: mit' 'tags:' '  - ai-coding' '  - i-lang' '  - ilang-protocol' '  - claude-code' '  - cursor' '  - developer-tools' '  - vibe-coding' 'pretty_name: AutoCode - AI Coding Skills Protocol' 'configs:' '  - config_name: default' '    data_files:' '      - split: train' '        path: data/*.jsonl' '---' '' > /tmp/hf-push/README.md
          awk '/^---$/{c++; next} c>=2' README.md >> /tmp/hf-push/README.md
          cd /tmp/hf-push
          git init -b main
          git config user.name "ilang-ai"
          git config user.email "ilang-ai@users.noreply.github.com"
          git add -A
          git commit -m "Sync from GitHub"
          git remote add hf https://ilangai:${HF_TOKEN}@huggingface.co/datasets/i-Lang/AutoCode
          git push hf main --force
